"""
Configuration module for Constitution Analysis Pipeline.

NOTE: This module exists for backward compatibility.
New code should import from the root config.py module instead.

All configuration is now centralized in config.py at the project root.
"""

# Re-export everything from the root config module
from config import *
